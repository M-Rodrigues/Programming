#include<bits/stdc++.h>
#define db(x) cerr << #x << " = " << x << endl
#define _ << " , " << 
using namespace std;
typedef long long ll;

int N, K, T, x, y;
vector<int> arestas[2001];

int main() {
    scanf("%d",&T);
    while(T--){
        scanf("%d%d",&N,&K);
        
        for (int i = 0 ; i <= N ; i++){
            
        } 
    }

    return 0;
}